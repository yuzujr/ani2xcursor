name: Release

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to release (e.g., v1.0.0)'
        required: true
        type: string

permissions:
  contents: write

jobs:
  prepare-release:
    runs-on: ubuntu-latest
    outputs:
      tag_name: ${{ steps.meta.outputs.tag_name }}
      version: ${{ steps.meta.outputs.version }}

    steps:
    - uses: actions/checkout@v4
      with:
        fetch-depth: 0

    - name: Create tag for manual release
      if: ${{ github.event_name == 'workflow_dispatch' && github.event.inputs.tag != '' }}
      run: |
        git config user.name "github-actions[bot]"
        git config user.email "github-actions[bot]@users.noreply.github.com"
        if git rev-parse "refs/tags/${{ github.event.inputs.tag }}" >/dev/null 2>&1; then
          echo "Tag '${{ github.event.inputs.tag }}' already exists."
        else
          git tag "${{ github.event.inputs.tag }}" "${GITHUB_SHA}"
          git push origin "refs/tags/${{ github.event.inputs.tag }}"
        fi

    - name: Resolve release metadata
      id: meta
      run: |
        if [ -n "${{ github.event.inputs.tag }}" ]; then
          TAG_NAME="${{ github.event.inputs.tag }}"
        else
          TAG_NAME="${GITHUB_REF_NAME}"
        fi

        VERSION="${TAG_NAME#v}"

        echo "tag_name=${TAG_NAME}" >> "$GITHUB_OUTPUT"
        echo "version=${VERSION}" >> "$GITHUB_OUTPUT"

  build:
    name: Build (${{ matrix.arch }})
    needs: prepare-release
    runs-on: ${{ matrix.runner }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - arch: x86_64
            runner: ubuntu-24.04
          - arch: aarch64
            runner: ubuntu-24.04-arm

    steps:
    - uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        sudo apt-get update
        sudo apt-get install -y libxcursor-dev libx11-dev libspdlog-dev libfmt-dev libstb-dev gettext locales squashfs-tools
        sudo locale-gen en_US.UTF-8 zh_CN.UTF-8

    - name: Validate translation files
      run: |
        msgfmt --check --check-format -o /tmp/ani2xcursor-zh_CN.mo locale/zh_CN.po

    - name: Build
      run: make

    - name: Prepare locale artifacts
      run: |
        mkdir -p build/locale/zh_CN/LC_MESSAGES
        msgfmt -o build/locale/zh_CN/LC_MESSAGES/ani2xcursor.mo locale/zh_CN.po

    - name: Download AppImage tools
      run: |
        set -euo pipefail
        curl -L -o linuxdeploy.AppImage \
          "https://github.com/linuxdeploy/linuxdeploy/releases/download/continuous/linuxdeploy-${{ matrix.arch }}.AppImage"
        curl -L -o appimagetool.AppImage \
          "https://github.com/AppImage/appimagetool/releases/download/continuous/appimagetool-${{ matrix.arch }}.AppImage"
        curl -L -o runtime \
          "https://github.com/AppImage/type2-runtime/releases/download/continuous/runtime-${{ matrix.arch }}"
        chmod +x linuxdeploy.AppImage appimagetool.AppImage

    - name: Build test binary
      run: |
        c++ -std=c++20 -Wall -Wextra -O2 -Iinclude \
          $(pkg-config --cflags spdlog xcursor stb) \
          tests/test_inf_parser.cpp src/cli.cpp src/inf_parser.cpp src/riff_reader.cpp src/ani_parser.cpp src/ico_cur_decoder.cpp src/xcursor_writer.cpp \
          -DANI2XCURSOR_VERSION=\"$(cat VERSION)\" \
          -o build/ani2xcursor_test \
          $(pkg-config --libs spdlog xcursor) -lpthread

    - name: Run tests
      run: ./build/ani2xcursor_test

    - name: Locale smoke test
      run: |
        set -euo pipefail
        BIN=./build/ani2xcursor
        locale -a

        VERSION="$("$BIN" --version)"
        HELP_EN="$(LC_ALL=en_US.UTF-8 LANGUAGE=en_US:en "$BIN" --help | head -n 1)"
        HELP_ZH="$(LC_ALL=zh_CN.UTF-8 LANGUAGE=zh_CN:en "$BIN" --help | head -n 1)"

        echo "VERSION=$VERSION"
        echo "HELP_EN=$HELP_EN"
        echo "HELP_ZH=$HELP_ZH"

        echo "$VERSION" | grep -q "^ani2xcursor "
        echo "$HELP_EN" | grep -q "^Usage:"
        echo "$HELP_ZH" | grep -q "用法"

    - name: Package AppImage
      run: |
        set -euo pipefail
        bash scripts/build-appimage.sh \
          "${{ matrix.arch }}" \
          "${{ needs.prepare-release.outputs.version }}" \
          "$PWD/linuxdeploy.AppImage" \
          "$PWD/appimagetool.AppImage" \
          "ani2xcursor-v${{ needs.prepare-release.outputs.version }}-linux-${{ matrix.arch }}.AppImage" \
          "$PWD/runtime"

    - name: Upload release artifact
      uses: actions/upload-artifact@v4
      with:
        name: release-${{ matrix.arch }}
        path: ani2xcursor-v${{ needs.prepare-release.outputs.version }}-linux-${{ matrix.arch }}.AppImage
        if-no-files-found: error

  package-source:
    name: Package source
    needs: prepare-release
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4

    - name: Build source archive
      run: |
        set -euo pipefail
        ASSET="ani2xcursor-v${{ needs.prepare-release.outputs.version }}-src.tar.gz"
        bash scripts/build-source-archive.sh "${{ needs.prepare-release.outputs.version }}" "$ASSET"

    - name: Upload source artifact
      uses: actions/upload-artifact@v4
      with:
        name: release-source
        path: ani2xcursor-v${{ needs.prepare-release.outputs.version }}-src.tar.gz
        if-no-files-found: error

  publish-release:
    runs-on: ubuntu-latest
    needs:
      - prepare-release
      - build
      - package-source

    steps:
    - name: Download release artifacts
      uses: actions/download-artifact@v4
      with:
        path: artifacts
        pattern: release-*
        merge-multiple: true

    - name: Create Release
      uses: softprops/action-gh-release@v1
      with:
        tag_name: ${{ needs.prepare-release.outputs.tag_name }}
        target_commitish: ${{ github.sha }}
        files: artifacts/*.tar.gz
        body: |
          ## ani2xcursor v${{ needs.prepare-release.outputs.version }}

          Convert Windows animated cursor themes (.ani/.cur) to Linux Xcursor format.

          ### Assets

          - `ani2xcursor-v${{ needs.prepare-release.outputs.version }}-linux-x86_64.AppImage`
          - `ani2xcursor-v${{ needs.prepare-release.outputs.version }}-linux-aarch64.AppImage`
          - `ani2xcursor-v${{ needs.prepare-release.outputs.version }}-src.tar.gz`

          ### Installation

          Download the AppImage for your CPU architecture, make it executable, and run it:

          ```bash
          chmod +x ani2xcursor-v${{ needs.prepare-release.outputs.version }}-linux-<arch>.AppImage
          ./ani2xcursor-v${{ needs.prepare-release.outputs.version }}-linux-<arch>.AppImage --version
          ```

          Optional system-wide install:

          ```bash
          sudo install -Dm755 ani2xcursor-v${{ needs.prepare-release.outputs.version }}-linux-<arch>.AppImage /usr/local/bin/ani2xcursor
          ```

          If FUSE is unavailable on your system:

          ```bash
          APPIMAGE_EXTRACT_AND_RUN=1 ./ani2xcursor-v${{ needs.prepare-release.outputs.version }}-linux-<arch>.AppImage --version
          ```

          ### Usage

          ```bash
          ani2xcursor /path/to/cursor/folder
          ```

          See [README.md](https://github.com/${{ github.repository }}/blob/main/README.md) for detailed documentation.
        draft: false
        prerelease: false
      env:
        GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
